# Game: Heavy Gear (PC, 1997) (script 0.1)
# Description:
# script to unpack .prj game file ("zlib" compression)
# Link:

comtype unzip_dynamic

for i = 0 < 19544
    GoTo 8 0 SEEK_CUR
    GetDString FN 4
    string FN l FN
    GoTo 2 0 SEEK_CUR
    get UNPACKED_SIZE LONG
    get SIZE LONG
    math OFFSET + 22   
    string NAME p "%i.%s" i FN      
    Clog NAME OFFSET SIZE UNPACKED_SIZE   
    math OFFSET + SIZE 
    GoTo OFFSET    
next i